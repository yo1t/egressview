node_modules/
.env
.env.local
.env.*.local
# 設定・履歴ファイル（秘密情報含む）
.egressview.json
.egressview*.pre-routers-v1.bak*
# MCP サーバー固有設定（EGRESSVIEW_TOKEN 等を含む）
.env.mcp
# 旧ファイル名（念のため除外）
.widemap.json
.widemap.connections.jsonl
.widemap.connections.jsonl.migrated
.widemap.db
.widemap.db-wal
.widemap.db-shm
.widemap-backups/
.widemap-upload-temp.db
.widemap.notes.json
.widemap.pid
.widemap-cert.pem
.widemap-key.pem
.egressview.connections.jsonl
.egressview.connections.jsonl.migrated
.egressview.db
.egressview.db-wal
.egressview.db-shm
.egressview-backups/
# demo DB snapshot is committed to git; runtime copy and WAL/SHM are excluded
.egressview.demo.runtime.db
.egressview.demo.runtime.db-wal
.egressview.demo.runtime.db-shm
.egressview.demo.config.json
.egressview.demo.test.config.json
.egressview.demo.db-wal
.egressview.demo.db-shm
.egressview-demo-backups/
.egressview-upload-temp.db
.egressview.notes.json
.oui_cache.txt
server.log
*.log
# デプロイ用スクリプト（環境固有）
scripts/deploy-ec2.sh
scripts/start.sh
# 個人的なバックログ
backlog.md
# SNS投稿用ドラフト（未公開）
SNS/
# 開発ツールのローカル設定（家庭内IP等を含む可能性）
.claude/settings.local.json
.claude/
.kiro/
# OS / エディタ
.DS_Store
.idea/
.vscode/
*.swp
*.swo
# 鍵関連（誤って置かれた場合の保険）
*.pem
*.key
id_rsa*
id_ed25519*
# ASH セキュリティスキャン: 出力はローカル専用だが、設定（suppression 込み）は
# CI ゲートで再現するためにトラッキングする。
# 親ディレクトリごと除外すると negation が効かないため .ash/* で中身を除外する。
.ash/*
!.ash/.ash.yaml
# テストのベースライン（実機データに依存）
test/fixtures/baseline.json
# Playwright 生成物
test-results/
playwright-report/
.egressview.pid
local-media/
redacted-media/

# Playwright test output
test-results/
playwright-report/
# TLS 証明書（自己署名・自動生成）
.egressview-cert.pem
.egressview-key.pem
# conntrack 検証環境（内部用・公開しない）
dev-conntrack/
# DB マイグレーション前自動バックアップ
*.pre-migration.*.bak
# GitHub traffic analysis artifacts (local only)
docs/github-traffic-last-month.svg
scripts/render-traffic-report.js
# Local-only Claude Code guidance (not shared with other contributors)
CLAUDE.local.md
